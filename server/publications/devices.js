import {Devices} from '/lib/collections';
import {Meteor} from 'meteor/meteor';
import {check} from 'meteor/check';

export default function () {
  // Meteor.publish('devices', function (devicesId) {
  //   return Devices.find(devicesId);
  // });
}
